"""Called by ensure all tables are current, stores the current exit liquidity info, USD reserves, etc """


def stub():

    # save current asset exposure

    # get (dex screener + coingecko) USD liqudity in other pools
    # (token, other_token, pair address)

    # these can happen at the same time
    # get tokemak exit liquidity quotes
    # save tokemak exit liquidity quotes

    pass
