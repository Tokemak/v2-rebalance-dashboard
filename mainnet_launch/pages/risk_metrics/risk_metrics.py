import pandas as pd

from mainnet_launch.constants import AutopoolConstants


def _fetch_current_percent_ownership_by_destination(autopool: AutopoolConstants) -> pd.DataFrame:
    """Fetch the current percent ownership by destination for the given autopool."""

    return pd.DataFrame()


def fetch_and_render_risk_metrics(autopool: AutopoolConstants) -> None:
    pass
