repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
        name: gitleaks (strict, no allowlist)
        args: ["protect", "--staged", "--verbose", "--redact"]
        stages: [pre-commit]
